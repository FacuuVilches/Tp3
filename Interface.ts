/*  La interfaz define la forma y el tipo de las propiedades y métodos que deben 
estar presentes en un objeto.
por ej: 
interface Persona {
  nombre: string;
  edad: number;
  saludar(): void;  (void es una función sin retorno.)
}
 */
